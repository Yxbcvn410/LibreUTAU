<Window x:Class="LibreUtau.UI.SplashScreen"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:local="clr-namespace:LibreUtau.UI"
        mc:Ignorable="d"
        ResizeMode="NoResize"
        WindowStyle="None"
        Title="SplashScreen" Height="450" Width="800">
    <Window.Background>
        <ImageBrush ImageSource="Assets/background.png" />
    </Window.Background>
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition />
        </Grid.RowDefinitions>
        <TextBlock x:Name="TitleText" Grid.Row="0" VerticalAlignment="Bottom" FontSize="48" Foreground="LightGray" />
        <TextBlock x:Name="ProgressText" Grid.Row="1" VerticalAlignment="Top" FontSize="15" Foreground="LightGray" />
    </Grid>
</Window>